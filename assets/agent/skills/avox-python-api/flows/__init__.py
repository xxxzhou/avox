# avox Python 端到端组合功能 (flows)
#
# 每个 .py 是一个可被 AI 直接 run_code 调用的完整功能, 多步编排 (如 抽音频→STT→翻译)。
# 与写法参考区分: flows = "做什么" (直接调 run(...) 拿结果, 完成一个任务)。
# 写法参考直接读 avox 源码 docstring (运行时 plugins/avox/*.py, 仓库 swig/python/avox/*.py)。
# 调用方式见 SKILL.md「flows 索引」。
