# 夜间施工进度 (2026-09-19 晚 → 09-20 早)

> 状态: 施工记录 · 上次核对: 2026-09-19 · 权威源: -

本文档是今夜施工的**唯一状态源**。上下文被压缩/换轮时先读本文档再干活, 只在此文档更新进度。
昨晚进度已归档: [../archive/夜间施工进度-0918晚.md](../archive/夜间施工进度-0918晚.md)。

## 用户拍板 (09-19, 本夜依据)

- **首发口径**: Apple 双端先行(iOS + macOS, universal purchase 一票通用), Windows 作
  开发验证平台; 商业化只接**买断 + BYOK 个人 API**, AI 资源包消耗型 IAP 推迟
  (随 P-17 超分入口一起 v1.1; OpenVINO 无 Apple GPU 后端是主因)。
- **机器前只写平台无关代码**: Mac/iOS 机器未到位, 本夜全部任务不依赖 Apple 构建与真机。
- **A-11 统一口径**: 只做离线超分, 实时超分维持搁置(README 状态行与 a11 文档已同步)。
- **跨仓分工**: panvox 会话今晚做 P-14/P-12/P-5/P-11/P-26(纯 Dart/StoreKit 面)与
  pvx_remote_* shim; 本会话只管 avox 仓实现。交互点见下「跨仓交接面」。

## 跨仓交接面 (今晚与 panvox 会话的依赖)

- 方向只有 **avox → panvox**, 且都是「头文件即契约」:
  1. T16 authExpired 回调面: AvoxBase.h `IRemoteSourceOb::onAuthExpired(sourceId)` +
     `IRemoteSource::reauthorize(entry, user, pass, token)` —— **本夜最先落地**:
     哪怕实现未完, 头文件+编译过+ctest 绿即先 commit, panvox 后半夜按新头写 shim 回调注册面。
  2. T17 样式覆盖接口签名: 同法先落公开头。
- 跨仓对账注记: 结束时**一次**写入对方仓, 不反复改; 对方仓 git index.lock 竞态一律等待让行(有先例)。

## 工作规则

- 单任务卡超 90 分钟未收敛: 停手记录现象/卡点, 转下一卡
- **「进行中」卡优先于「未开始」卡续作**(T20 半场示例: 下轮先接 SmbSource 复用, 不跳 T21)
- 构建 `python build_windows.py` (增量); 单测 `ctest --test-dir build/windows/avox --output-on-failure -C Release`
- 播放回归 `cd D:/Work/github/avox-test && python script/testenv/play_regress.py --offline`
- 允许 `git commit` (首行≤50字), **禁止 push**, 禁 reset/rebase, 禁动 `3rdparty/` 子模块
- 公开头改动「只增不改」纪律, 动 AvoxBase.h/AvoxPlayer.h 必同步 SWIG 口径(touch common.i)
- 拿不准的口径: 按计划文档推荐项执行并记录决策; 需要用户拍板的写「待拍板」跳过不空转

## 任务卡总表

| 卡 | 任务 | 状态 | 备注 |
|---|---|---|---|
| T16 | A-5 §1 authExpired 回调链: AvoxBase.h 只增(onAuthExpired/reauthorize) + DavSource 会话 invalid/武装 + RemoteCode 流转 + SWIG 口径; **头最先落** | 完成 00:24 (`2cacdef`+`3cf565d`) | panvox shim 依赖此头(**已可接**); 30s closed 语义后置; 用例 dav-auth-expired 仍随 T5 打开 |
| T17 | A-1 T3 ASS 样式覆盖接口: 签名按 [a01](backlog/a01-ass-pgs.md) 施工方案落公开头(只增) + ass 渲染接入 + SWIG; 头先落 | 完成 01:21 (`f2c328e`+`0936284`) | panvox P-5 样式 UI(**已可接**: setAssScale/setAssFont); 真渲染取证待素材走查 |
| T18 | closeTap 竞态: AudioRender::closeTap vs 解码线程 render→push 加小锁/快照指针 | 完成 02:10 (`32f8905`) | 崩溃隐患, 小卡 |
| T19 | copy_head 头文件依赖: 修 install/include 不随头提交刷新(09-18 vtable 崩溃事故根) | 完成 03:08 (`cee89e2`) | 构建纪律, 小卡 |
| T20 | A-5 T4 目录列表缓存(DavSource/SmbSource 会话级连接复用 + TTL 树缓存) | 进行中 03:44 (`fd4fc87` dav 半场) | **下轮优先续作 SmbSource 复用**; 秒开指标属 avox-test 侧 |
| T21 | A-4 Apple 直通桥源码盲写(CVPixelBuffer/IOSurface 导出 + Flutter iOS 纹理注册, 抄 VR/Android 车道结构) | 未开始 | 后续夜次卡; 首发可不依赖(CPU 泵可上); 真机到位即可验 |

## 轮次记录 (每卡完成追加)

| 时间 | 卡 | 记录 | 结论 |
|---|---|---|---|
| 2026-09-20 00:24 | T16 | 头 `2cacdef` 最先单提(panvox 可接); 实现 `3cf565d`: DavSource 三态(中途401/403→authExpired(-4), onAuthExpired 武装位至多一次), reauthorize 凭据快照热更新+refresh 重取续播, IOParseDav 断链桥等 30s 续播; 构建+ctest 2/2 绿; 清了遗留 playtest.exe 的 DLL 锁(LNK1104) | 完成 |
| 2026-09-20 01:21 | T17 | 头 `f2c328e` 先落(ISubtitle 尾 + setAssScale/setAssFont, panvox P-5 可接); 实现 `0936284`: IAssOverlay 只增默认实现 → AssOverlay(font_scale + force_style, 新轨重应用) + SubtitleView 存档/建通道补发; 真实 libass 分支编译链接确认; 构建+ctest 2/2 绿; 真渲染取证待素材 | 完成 |
| 2026-09-20 02:10 | T18 | `32f8905`: audioTap unique_ptr→shared_ptr + tapMtx 快照指针, render/onAudioProcess 锁内取快照锁外 push, closeTap 锁内 move-out 锁外 close; AudioTap bRunning/setClose 兜底中途入队; push 不持锁防反压互等; 构建+ctest 2/2 绿 | 完成 |
| 2026-09-20 03:08 | T19 | `cee89e2`: copy_head 追加 ALL custom target(构建期 copy_if_different), 头一变即同步 install/include; 修前实测 stale(两新头 0 命中)修后 2/2 命中; 构建+ctest 2/2 绿 | 完成 |
| 2026-09-20 03:44 | T20 | `fd4fc87` dav 半场: propfind 会话级 Client(keep-alive+失败重建一次) + 目录 TTL 缓存(setParam listCacheTtl, 命中同步回填, refresh 链式同步, 容量 64); 构建+ctest 2/2 绿; **剩 SmbSource 复用留明晚**(90 分钟规则收账) | 进行中 |
