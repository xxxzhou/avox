# 夜间施工报告 (2026-09-18 深夜 → 09-19 早7点)

> 状态: 施工记录 · 上次核对: 2026-09-19 · 权威源: -

施工全程明细见 [夜间施工进度.md](夜间施工进度.md) (唯一状态源, 本文为其汇总)。

## ① 总览

用户深夜拍板 A-1 走方案 A(枚举+查询)、A-3 硬解优先软解兜底, 授权本会话「只管完成」,
并行另一会话在 avox-test 仓设计测试。今夜 **13 张任务卡全部收口**, 产出 **7 个提交**
(A-1 信号暴露 / A-3 三平台 VP9 硬解 ×2 / A-2 probe 降档 / 超分零头一批 / A-5 契约设计
+ A-6 守卫宏 / 计划文档回写)。**验证口径**: ctest 2/2 全绿每卡必过; 离线回归最终
41/42 (唯一红 file-vp9 为 avox-test 新用例的时序标定项, 引擎证据见 §④); VP9 硬解
本机实测命中 (decoder=ff_vp9_dx11, hw=1)。未 push (遵守惯例, 待用户收尾)。

**并行会话交互 (重要)**: 另一会话今晚在 avox-test 大量加用例 (vp9 双路/seek 探针/
probe-field/dav/sub-enc-expose 等 62 条) 并迭代阈值, 引擎仓也有在途改动
(Linux VAAPI/FFVkDecoder/ffmpeg linux 脚本/TranscodeRecorder close 重构)。本会话
未 revert 未代提交; 一次例外见 §③ 吞提交事件。

## ② 逐任务卡结果

| 卡 | 结论 | 证据 |
|---|---|---|
| T1 A-1 信号暴露(方案A) | **完成** (`1608887`) | `SubtitleEncoding` 公共枚举 + `normalizeSubtitleText` bool→枚举(修 UTF-16 歧义坑) + `ISubtitle::getFileEncoding()` 带默认实现透出(只增不改); SWIG 四语言构建期再生成实证(SubtitleEncoding.cs 等); 白盒单测升级枚举语义。**落点偏离**: 原定 getSubtitleDesc 扩字段对外挂不适用(外挂不经源), 改 ISubtitle 查询, 见 a01 文档 |
| T2 A-3 Windows VP9 | **完成** (`1c195f5`) | D3D11VA 注册 + onVaild GPU profile 探测(MS-PEPF GUID) + **VDecoderTask 选型回退**(硬解失败→软解名, 全编码生效); 本机 RX 9070 XT 实测 select ff_vp9_dx11 / renderType:d3d11 / seektest PASS (seek 14ms) |
| T3 A-3 Android VP9 | **完成** (`19e4729`, 待真机) | mime video/x-vnd.on2.vp9 + dlsym AMediaCodec_getName(API28+) 按名排除 c2.android./omx.google. 软实现; NDK 单 TU 编译零错误 |
| T4 A-3 Apple VP9 | **完成** (`19e4729`, 待 mac 编译+真机) | onVaild 版本(iOS14/macOS11)+VTIsHardwareDecodeSupported 双探测; **bMustVcc 对 vp9 关闭**(否则 annexb 重排破坏 in-band 帧); 无工具链仅保语法 |
| T5 A-2 probe 降档 | **完成** (`ed75f49`) | 本地索引容器快档(probesize 1MB/analyze 1s); **保底字段缺失恢复默认参数补查一次**(宽高/pix_fmt/fps/采样率/声道); 实测 find_stream_info 3-14ms, 10bit HDR 快档直接 yuv420P10(A-12 依赖无损) |
| T6~T10 超分零头 | **完成** (`843b4b2`) | G1 色彩透传(删 bt601 硬编码)/G14 纯 SEI AU 丢弃(首 NAL SEI 才整包扫 VCL, 不误杀混合包)/G13 muxer 排空 10s 有界(超时弃排保 moov)/G-frames 方案①(onRenderOut 补 image 分支派发)/G15 口径=增强优先+warn 明示/FindRealEsrgan.cmake 仓内优先。enhancetest 实证 **frames 0→120**、完整收尾 rc=0 |
| T11 A-5 T1 契约设计 | **完成** (`44cf620`) | a05 文档定稿: authExpired 三态授权流/onAuthExpired 回调(只增不改)/refresh 直链重取续播通道/IOParseDav 结构(Torrent lookahead 参照)/alist token Header 注入/接口清单/BDMV 预留口 |
| T12 A-6 T4 版本守卫 | **完成** (`44cf620`) | FFCommon.hpp avcodec major==63 static_assert + AVOX_FF_ALLOW_VERSION_BUMP 逃生口 |
| T13 收尾回写 | **完成** (`feef292`) | current.md/a01/a02/a03/backlog README 全部回写到真实状态; 昨晚进度文档归档 doc/archive/ |

**a02-T3 seek 精确化有意未动**: 实测 seek 31ms/落位 0ms, 无冗余 GOP 解码症状;
avox-test 新 seek 探针(file-h264-seek/file-vp9-seek)正在采基线, 有数据后再优化。

## ③ 夜间提交清单 (均未 push)

| commit | 说明 |
|---|---|
| `1608887` | feat(subtitle): 编码探测返枚举, ISubtitle查询透出 (a01-T2) |
| `1c195f5` | feat(decoder): VP9 D3D11VA硬解注册, 硬解失败选型回退软解 (a03-T2) |
| `19e4729` | feat(decoder): Android/Apple VP9硬解注册, 软实现排除与VT探测 (a03-T3/T4) |
| `ed75f49` | feat(io): 本地索引容器probe降档, 保底字段缺失回退补查 (a02-T2) |
| `843b4b2` | fix(recorder): 超分G1色彩透传/G14纯SEI丢弃/G13排空兜底/frames计数 |
| `44cf620` | feat(ffmpeg): avcodec版本守卫宏, a05契约设计定稿 |
| `feef292` | docs(plan): M1五项进度回写, 只留真未完成项 |

**吞提交事件 (声明)**: `1c195f5` 因读写竞态误吞并行会话在 AVTrack.cpp 的 Linux
VAAPI h264/h265 解码名映射改动 (内容正确, 但提交信息未体现)。此后本会话改用
hunk 级暂存保护对方工作 —— TranscodeRecorder.cpp 的并行 close 重构
(bStopPending/bDrainPhase, 属录制层 G13 变体) 已成功隔离在 `843b4b2` 之外,
截至报告时仍在其工作区未提交, 归对方会话处置。

## ④ 遗留与建议下一步

**本机不可测, 待 CI/真机**:
1. A-3: Android 真机跑 file-vp9 (软实现排除逻辑实测); Apple mac 编译 + 真机
   (VTIsHardwareDecodeSupported 实测)。
2. A-6: 四平台回归 + UE 链路 (不变)。

**avox-test 侧 (并行会话口径)**:
3. `file-vp9` 矩阵内偶发红: 判定条件 (playing && fps>0 && pos>1500) 在 12s 窗口 +
   晚位槽高负载下 miss (fps 表预热时序); 引擎侧证据: 单跑三轮全 PASS 且
   decoder=ff_vp9_dx11 hw=1、邻居 h264 同车道绿。建议放宽 fps 预热窗口或按
   state_at_1500 采样点改判。`file-vp9-seek` 单跑 PASS 同理。
4. 字幕用例新阈值 (dark-win0-lit) 首晚标定, 与素材再生成联动。

**外部依赖 (不变)**: PGS/dts/Hi10P/BDMV 样片; W38 原始样片 (A-3 已闭环大半,
样片到位仅剩定性归档); 模型权重 LFS 分发方案 (基建决策)。

**下一步代码建议序**: A-5 T2 IOParseDav (契约已定, panvox P-4 硬依赖) →
a02-T3 seek 精确化 (等基线) → A-1 T3 样式/延迟接口。
