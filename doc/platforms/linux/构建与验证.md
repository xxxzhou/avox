# Linux 构建与验证 (WSL/桌面)

> 状态: 有效 · 上次核对: 2026-09-16 · 权威源: -


> 2026-09 首次跑通: WSL2 Ubuntu 24.04 全量构建 + X11/Vulkan 窗口播放 + PulseAudio 音频 + 播放回归矩阵离线子集全绿。

## 状态总览

| 能力 | 状态 | 说明 |
|------|------|------|
| 全量构建 | ✅ | `python3 build_linux.py` (ZLMediaKit/fdk-aac/freetype + SDK) |
| 软解播放窗口 | ✅ | X11(XWayland) + Vulkan swapchain, FFmpeg 软解 → VkVideoRender |
| PulseAudio 音频 | ✅ | WSLg 自带 Pulse server; 桌面发行版 PipeWire 兼容 pulse 协议 |
| VAAPI 硬解 | ✅编译/运行待真机 | FFVADecoder: VAAPI 解码 → av_hwframe_transfer_data 映射 CPU NV12 → 软解链路; WSL 无 /dev/dri 自动降级软解 |
| SDK 自建窗口 | ✅ | setSurface(nullptr) 时 SDK 自建 X11 窗口 (对齐 win32 契约) |
| Wayland 原生 | ✅ | xdg_shell + VkWaylandSurfaceKHR; `AVOX_LINUX_WM=wayland` 或纯 Wayland 会话 (无 DISPLAY) 自动启用 |
| 离屏截图 | ✅ | 矩阵 shot 用例通过 (Linux 强制 Vulkan 车道); 上游"离屏 fetchFrame=0"的说法在 Linux 已不成立 |
| 原生渲染器 (setVulkan(false)) | ❌ 未实现 | 无 DX11/Metal 对应物; 相关矩阵用例(yuvout 车道B)在 Linux 结构性跳过 |
| h264 软编 | ❌ 环境限制 | LGPL FFmpeg 白名单无视频编码器(Windows 靠 h264_mf), 转码录制用例不可用 |

## 依赖安装 (Ubuntu 24.04)

```bash
sudo apt install build-essential cmake python3 ninja-build pkg-config \
  libx11-dev zlib1g-dev libpng-dev libbz2-dev libpulse-dev libva-dev \
  mesa-vulkan-drivers vulkan-tools ffmpeg pulseaudio-utils
```

FFmpeg 用 `3rdparty/library/linux/ffmpeg` 预编译包 (构建方法见 `script/ffmpeg/build_ffmpeg_linux.sh`), 不用系统包。

## WSL 注意事项 (重要)

- **swap**: 默认 `.wslconfig` 常见 `swap=0`, 编译高峰(大 TU 并行 g++)会把 16G 内存打穿触发 OOM-killer 集体击杀, 表现为构建中途无声消失。根治: `.wslconfig` 里 `swap=16GB`; 本机已用 `wsl -u root` 建 `/swapfile` 并写入 `/etc/fstab`(`/swapfile none swap sw 0 0`, 否则 VM 重启后 swap 丢失)。并行度可用 `CMAKE_BUILD_PARALLEL_LEVEL` 压低 (-j1 稳, -j4 易把 VM 抖死)。
- **VM 会随机重启/清 /tmp**: 改 `.wslconfig` 前的默认行为, 长任务务必落盘到 `~` 而非 `/tmp`; 长编译建议附着会话跑且死了重发 (增量会续)。
- **CMake find_* 偶发失灵**: 本机 find_path/find_library 对存在的系统文件返回 NOTFOUND (缓存或环境因素), Wayland 检测因此改用 `EXISTS` 硬探测。若 configure 结果可疑, `cmake --fresh` 清缓存重配, 并确认 build 日志里有 "Wayland enabled"/"PulseAudio found"。
- **改了 *.cmake 必须显式重 configure**: make 的自动重建规则在 9p/UNC 同步场景下可能不触发, 直接 `cd build/linux/avox && cmake ../../../ ...`。
- **构建目录必须在 ext4** (`~/avox`), 别在 `/mnt/d` 上编译 (9p IO 慢一个数量级)。同步源码可用 `cp` 走 `//wsl$/Ubuntu/home/<user>/avox/`。
- **WSLg 显示**: `DISPLAY=:0` 即 XWayland, 窗口直接出现在 Windows 桌面; 截窗验证可用 `xwd -name avox | ffmpeg -i pipe:0 out.png` (root 抓取 BadMatch, 必须按窗口抓)。
- **验证 Vulkan**: `vulkaninfo --summary` 应看到 dzn(Paravirtualized D3D12) 或 lavapipe(CPU)。

## 构建与运行

```bash
cd ~/avox && python3 build_linux.py          # 产物 build/linux/avox/install/x86_64/
ctest --test-dir build/linux/avox            # 单测
# 窗口播放 (宿主建窗口):
cd build/linux/avox/install/x86_64 && LD_LIBRARY_PATH=. ./linuxtest <url或文件>
# 播放回归矩阵离线子集 (无 ZLM, CI 用):
python3 script/testenv/play_regress.py --offline
```

离线子集在 Linux 上额外跳过 `shot`/`yuvout-h264-soft`(需原生渲染器) 与 `rec-transcode-*`(需视频编码器), 见 `script/testenv/play_regress.py` 的 `LINUX_OFFLINE_SKIP`。

## 关键代码位置

| 模块 | 位置 |
|------|------|
| X11 窗口 | `src/avox_linux/X11Surface.cpp`, `LinuxHelper.cpp` |
| Vulkan Xlib surface / SDK 自建窗口 | `src/avox_vulkan/vulkan/VkWindow.cpp` (`initVkSurface`, `onInitWin` Linux 分支) |
| Vulkan 实例扩展 | `src/avox_vulkan/VkCommon.cpp` (`defArgs` 补 `VK_KHR_XLIB_SURFACE_EXTENSION_NAME`) |
| 窗口尺寸轮询 | `src/avox/video/Window.cpp` (`getSurfaceSize` Linux 分支) |
| PulseAudio 输出 | `src/avox_linux/PulseAudioRender.*`, 注册见 `AvoxManager.cpp` |
| Wayland 窗口 | `src/avox_linux/WaylandSurface.*` (xdg_shell, 协议码由 wayland-scanner 配置期生成到构建目录); 后端选择见 `LinuxHelper.cpp` (`AVOX_LINUX_WM=x11|wayland`) |
| VAAPI 硬解 | `src/avox_ffmpeg/decoder/FFVADecoder.*` |
| 播放回归 runner | `platform/linux/playtest/` (复用 `tests/playmatrix/HostMain.cpp`); 全量一键: `script/testenv/run_linux_full.sh` (本地起 MediaServer 高端口+推流+全矩阵) |

## 待办

- [ ] VAAPI 真机验证 (Intel/AMD 核显, 需真 Linux + /dev/dri)
- [ ] VAAPI→Vulkan 零拷贝 (DMA-BUF 导入, 现 CPU NV12 映射)
- [ ] Linux 原生渲染器 (EGL/GLES 或禁用报错), 补齐车道B
- [ ] Wayland 原生 surface (`vkCreateWaylandSurfaceKHR`), 当前走 XWayland
