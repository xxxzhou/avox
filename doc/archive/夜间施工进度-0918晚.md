# 夜间施工进度 (2026-09-18 晚 → 09-19 早7点)

> 状态: 归档 · 上次核对: 2026-09-19 · 权威源: -

本文档是今夜施工的**唯一状态源**。上下文被压缩/换轮时先读本文档再干活, 只在此文档更新进度。
明早 07:00 用户收尾。昨晚进度已归档: [../archive/夜间施工进度-0917晚.md](../archive/夜间施工进度-0917晚.md)。

## 用户拍板 (09-18 深夜, 本夜依据)

- **A-1 走方案 A**: `normalizeSubtitleText` 返回值 bool→枚举
  `{utf8, utf8BomStripped, gbkTranscoded, utf16Raw, unknown}`, `SubtitleFile` 记录,
  `SubtitleDesc` 扩 `encoding` 字段透出; 顺带修 bool 歧义坑 (UTF-16 分支 false 与
  「本来就是 UTF-8」不可区分); 动公开头须同步 SWIG 四语言 (java 构建期生成不入库)。
- **A-3 硬解优先**: 三平台能硬解就硬解 (Win D3D11VA / And MediaCodec / Apple VideoToolbox),
  软解作退化项/可选项; 不走 Vulkan 路线。Windows 本机实测, Android 编译验证 (NDK 在则编),
  Apple 只能写代码标注待真机/mac 编译。
- **其余项**按用户「把这些提到的问题都解决」口径执行: A-2 T2 (保底字段硬要求)、
  超分 P0 零头 (G1/G13/G14/frames=0 方案①)、A-5 T1 契约设计文档、A-6 T4 版本守卫宏。
- **并行会话**: 用户另开会话在 avox-test 仓**设计测试任务**; 本会话只管 avox 仓实现。
  avox-test 仓可能有并行改动: 不 revert 对方, 冲突就记录跳过。

## 工作规则

- 单任务卡超 90 分钟未收敛: 停手记录现象/卡点, 转下一卡
- 构建 `python build_windows.py` (增量); 单测 `ctest --test-dir build/windows/avox --output-on-failure -C Release`
- 播放回归 `cd D:/Work/github/avox-test && python script/testenv/play_regress.py --offline`
- 允许 `git commit` (首行≤50字), **禁止 push**, 禁 reset/rebase, 禁动 `3rdparty/` 子模块
- 新测试用例**不在本会话设计** (用户另一会话负责 avox-test); 本仓 `tests/test_*.cpp` 白盒单测可加
- 拿不准的口径: 用户已授权「只管完成」, 在授权范围内按计划文档推荐项执行并记录决策

## 任务卡总表

| 卡 | 任务 | 状态 | 结果一句话 |
|---|---|---|---|
| T1 | A-1 方案A 信号暴露 (枚举+desc扩字段+SWIG) | **完成** | 提交 `见git log`; ctest 2/2 + 回归 33/33 |
| T2 | A-3 Windows D3D11VA VP9 + 实测 | **完成** | `1c195f5`; 本机 RX 9070 XT 实测 select ff_vp9_dx11, renderType:d3d11, seektest PASS |
| T3 | A-3 Android MediaCodec VP9 | **完成**(编译冒烟过, 待真机) | `19e4729`; dlsym AMediaCodec_getName 排除软实现 |
| T4 | A-3 Apple VideoToolbox VP9 (待真机) | **完成**(代码落地, 待 mac 编译+真机) | `19e4729`; onVaild 版本+VTIsHardwareDecodeSupported 探测; bMustVcc 对 vp9 关闭 |
| T5 | A-2 T2 probe 降档 + 保底字段 | **完成** | `ed75f49`; 本地 mp4 find_stream_info 3-14ms, 10bit pix_fmt 保底实证 yuv420P10 |
| T6 | 超分 G1 源色彩空间透传 | **完成** | `843b4b2`; shader 矩阵与 encoder tag 同源透传, 无标记源 bt601 兜底不变 |
| T7 | 超分 G14 纯 SEI 访问单元丢弃 | **完成**(逻辑落地, 原缺陷源无样片未复验) | `843b4b2`; 首 NAL SEI 才整包扫 VCL, SEI+slice 不误杀 |
| T8 | 超分 G13 收尾超时兜底 | **完成**(muxer 层排空 10s 有界; 录制层 close 重构由并行会话在途) | `843b4b2`; onClose 恒写 trailer 保 moov |
| T9 | 超分 frames=0 方案① | **完成** | `843b4b2`; enhancetest 实证 frames 0→120 |
| T10 | FindRealEsrgan.cmake + G15 | **完成** | `843b4b2`; 仓内 assets 优先; G15 定口径: 增强优先, 显式 setOutVideo 被忽略时 warn 明示 |
| T11 | A-5 T1 契约设计文档 | **完成** | `44cf620`; a05 文档新增契约设计节 |
| T12 | A-6 T4 版本守卫宏(可选) | **完成** | `44cf620`; avcodec major 63 static_assert + AVOX_FF_ALLOW_VERSION_BUMP 逃生口 |
| T13 | 收尾: 计划文档状态更新 + 夜间报告 | **完成** | `feef292` + 报告见 [夜间报告-2026-09-19.md](夜间报告-2026-09-19.md) |
| T14 | A-1 T3 字幕延迟接口 (加卡, 03:00) | **完成** | `21491dc`; 回归 41/41 全绿零红 |
| T15 | 上午会话: 提交并行会话夜产 + A-5 T2 IOParseDav (加卡, 08:08) | **完成** | 并行夜产由其自身提交流收尾(d38f8af~96b5711); T2 `29c2cd6` 本机实测 dav:// PASS, 回归 44/44 |

## 轮次记录 (每卡完成追加)

| 时间 | 卡 | 做了什么 | 结果 |
|---|---|---|---|
| 09-18 23:45 | 开工 | 拍板入档, 归档昨晚进度 (含状态头/链接更新), 建本看板 | 准备就绪 |
| 09-19 00:20 | T1 | A-1 方案A: `SubtitleEncoding` 公共枚举(AvoxCodec.h, XX表+`getSubtitleEncodingName`导出, 实现落 CharsetConvert.cpp 供白盒单测自包含) / `normalizeSubtitleText` bool→枚举(修 UTF-16 歧义坑) / SubtitleFile 记录+getter / `ISubtitle::getFileEncoding` 带默认实现追加(只增不改) / SubtitleView 覆写 / 单测升级枚举语义。**口径偏离记录**: 勘察原文「随 getSubtitleDesc 扩字段」对外挂不适用(外挂不经源, BaseSource 注释明示), 信号落点改 `ISubtitle::getFileEncoding`(产品已持有该对象, 查询即得); ISTrackDesc 未动。SWIG 四语言: 产物为 gitignore 构建期再生成件, 头文件+common.i 已含新接口, 本机构建实证 C#/python/nodejs 均再生成(SubtitleEncoding.cs 等), java 构建期生成不入库。UseSWIG 不跟踪头依赖, 改头后需 `touch swig/common.i` 再构建 | **T1 完成** (`1608887`); ctest 2/2 绿, 离线回归 33/33 绿 (avox=96bc9ef+工作区); 插曲: 枚举名实现先放 Avox.cpp 致测试 LNK2019, 移 CharsetConvert.cpp 解决; 工作区发现并行会话在途改动 (Video.cpp P010 交错/Dx11CSVideoRender), 未 revert 未代提交, 回归已覆盖含其改动的产物 |
| 09-19 00:28 | T2 | VP9 D3D11VA: 注册表+AVOX_FFDX11_VP9_DECODER+onVaild GPU profile 探测(MS-PEPF GUID 本地定义, dxva.h DEFINE_GUID 链不上); **VDecoderTask 选型回退**: 首选名 init/setContext 失败→回退软解名→原兜底首项, 全编码生效(a12 回退框架的先行手动兜底)。**事件: 本提交误吞并行会话 AVTrack.cpp 的 Linux VAAPI h264/h265 映射在途改动**(读写竞态, 历史不可重写未拆分, 已在此声明) | **T2 完成** (`1c195f5`); 本机实测 VP9 硬解命中(renderType:d3d11, seektest PASS, seek 14ms); ctest 2/2 + 回归 30/30 绿 |
| 09-19 00:55 | T3/T4 | Android: mime video/x-vnd.on2.vp9 + dlsym AMediaCodec_getName(API28+, 编译目标26) 按名排除 c2.android./omx.google. 软实现, 命中软实现 onClose 回退; onPreDecoder 补 vp9 无 csd 分支。Apple: 注册+onVaild(iOS14/macOS11 + VTIsHardwareDecodeSupported) + **bMustVcc 对 vp9 置 false**(否则 annexb 重排破坏 in-band 帧) + CMVideoFormatDescriptionCreate 直建(srcDesc 宽高)。并行会话仍在途: FFVkDecoder/ffmpeg linux 脚本/VAAPI 相关未动 | **T3/T4 完成** (`19e4729`); Android 单 TU NDK clang 编译零错误(aarch64-android26); Apple 无工具链只保语法, 待 mac 编译+真机; ctest 2/2 绿 |
| 09-19 01:00 | T5 | IOParseFF open 段: 本地索引容器(mp4/mov/matroska/avi/flv 且无 scheme) probesize 1MB + analyze 1s 快档; 保底字段(视频宽高/pix_fmt/fps, 音频采样率/声道)任一缺失恢复默认参数补查一次; 网络/ts/HLS 不动。实测: find_stream_info 3-14ms(1-12s 素材), 10bit HDR mp4 快档直接出 yuv420P10 无需补查 | **T5 完成** (`ed75f49`); 回归引擎用例全绿 |
| 09-19 01:55 | T6~T10 | 超分零头一批: G1 透传(删 bt601 硬编码, cs 从源 desc 直取) / G14(IOMuxer 纯非VCL AU 判定+丢弃, annexb/avcc 双布局扫描) / G13(IOMuxer onRunTask 停止后排空 10s 有界, 超时弃排保 trailer; 注: 阻塞点在单次 IO 写内时此兜底无效, 根因仍开放) / G-frames(SurfaceRenderVk enableImage 存 imageOutUser, onRenderOut 补 image 分支派发) / G15 口径拍板: **增强优先, 显式 setOutVideo 被忽略时 warn 日志明示**(级联缩放留接口注释) / FindRealEsrgan.cmake 仓内 assets 优先。**hunk 级暂存**: TranscodeRecorder.cpp 并行会话有在途 close 重构(bStopPending/bDrainPhase 收尾块), 只 staged 我的 2 个 hunk。enhancetest 实测: frames 0→120, media 11967ms 完整收尾 rc=0。**回归误报澄清**: 全量回归 fail=10 中 7 字幕红 = avox-test 新阈值判据(dark-win0-lit, 同引擎上轮 PASS); file-vp9/file-h264-seek 红 = 新用例晚位槽时序敏感(getFps 预热), **单跑全 PASS 且 file-vp9 实证 decoder=ff_vp9_dx11 hw=1**; probe-field-rawh264 = 素材缺位 | **T6~T10 完成** (`843b4b2`); ctest 2/2 绿 |
| 09-19 02:45 | T11~T13 | A-5 T1 契约设计定稿(a05 文档: authExpired 三态流/refresh 续播通道/IOParseDav 结构/alist Header 注入/接口清单只增不改/BDMV 预留); A-6 T4 FFCommon static_assert 守卫; a02-T3 seek 精确化评估: 实测 seek 31ms/落位 0ms 无冗余 GOP 症状, 且 avox-test seek 探针正在采基线, 凌晨不动 seek 路径, **等基线后做**。收尾: current.md/a01/a02/a03/backlog README 状态回写。插曲: 构建被并行会话 playtest 锁 avox.dll 两次, 等待自愈未杀进程 | **T11~T13 完成** (`44cf620`/`feef292`); 最终回归 41/42 (唯一红 file-vp9 为用例时序标定项, 见报告) |
| 09-19 03:05 | T14 | A-1 T3 字幕延迟: `ISubtitle::setDelay(ms)` 带默认实现追加; SubtitleView 平移内容选择——外挂文本 getCurrent(pts-delay)/ASS 预喂窗口与 render(subPts)/PGS 画布按 ptsMs 到期放行(**delay=0 原无条件快照路径零变化**; setPgsCanvas 空画布补 ptsMs 保留, 否则清屏包提前穿帮); ASR 实时口播不平移。回归 41/41 全绿零红(file-vp9 本轮转绿, 时序标定论证实) | **T14 完成** (`21491dc`); ctest 2/2 绿 |
| 09-19 08:55 | T15 | ① 工作区并行夜产(Linux FFmpeg 重编/Vulkan 备选硬解/close 重构/样例)验证后由并行会话自身提交流收尾(d38f8af/1482add/d0d6ea2/96b5711), 本会话期间与其 index.lock 竞态多次, 均等待让行; ② **A-5 T2 IOParseDav**: 新组件(httplib Range+4MB 预读窗口+Basic 认证+0-0 探测), IoPlan 加 dav, MediaPlayer dav://davs:// 自动路由, 注册挂 AVOX_REMOTE_DAV(需 ffmpeg, 独立于 libsmb2)。**关键修复: EOF 停放**(bEof/bEofReset, IOParseFF 同款)——首版抄 Smb 的 EOF break 写法在小文件「起播即读完+seek」场景管道静止, 实测确诊后修复。**附带发现: IOParseSmb 读循环 EOF 直接 break, 同款隐患未修**(本机 libsmb2 未编入无法验证) | **T15 完成** (`29c2cd6`); 本机 range 服务器实测 dav:// 播放+seek PASS(seek 109ms, 6 帧全活); ctest 2/2 + 回归 44/44 绿 |
