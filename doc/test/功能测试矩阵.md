# 功能测试矩阵

> 单一事实源: 每个波次跑完只更新本文件。判定行约定与运行命令见
> [script/testenv/README.md](../../script/testenv/README.md)，汇总用 `collect_verdicts.py`。
> 环境与插件关系、测试/修复流程、上架清单见: [环境与插件总览](环境与插件总览.md) /
> [测试与修复工作流](测试与修复工作流.md) / [发布检查清单](发布检查清单.md)。

## 测试环境

| 环境 | 说明 | 状态 |
|------|------|------|
| Windows 本机 | 构建机 + adb（Redmi K70 Ultra, Android 16, arm64-v8a）+ ZLM MediaServer(80/554/1935/8000) | 就绪 |
| 本机流源 | `python script/testenv/push_streams.py`（live/avox=H265, live/avox264=H264） | 就绪 |
| Mac `ssh mac` (192.168.68.219) | Xcode 26.1 arm64；仓库 `/Volumes/pssd/work/github/avox`；iOS 设备需解锁+信任 | macOS 链路就绪 (见 W5); iOS 待救活 |
| Unity / UE 编辑器 | 本机未安装（C/D/Q 盘均无） | 待装 |

## 波次

| 波次 | 内容 | 状态 |
|------|------|------|
| W0 | 桌面底座: ctest + functest/vulkantest 关键项 | ctest ✓ 2026-09-09, functest 未跑 |
| W1 | Windows 三引擎冒烟（Godot ✓ / Unity / UE） | Godot 完成 2026-09-09 |
| W2 | Android 真机（adb 深链驱动全协议） | 待重出 APK (avox:// 深链) |
| W3 | WebRTC 专项（重连/TURN/DataChannel/弱网/长稳/延迟打点） | 拉流 H264/H265 ✓ 2026-09-09, 其余未跑 |
| W4 | 稳定/边界（7×24 soak、后台锁屏来电、多实例、4K/10bit） | 未跑 |
| W5 | macOS 全协议 + iOS 救活 + VideoToolbox/Metal 真机专项 | macOS ✓ 2026-09-10 复测 (onFrame Metal 缺口待修), iOS 未跑 |

## W1 — Windows Godot 工具箱（2026-09-09 实测）

| case | 结果 | 备注 |
|------|------|------|
| push-live-avox-rtsp/rtmp/hls/ts | PASS | testenv 推流自检 |
| push-live-avox264-rtsp/rtmp/ts | PASS | H264 路 |
| url-rtsp-…-live-avox (H265) | PASS | headless 起播 178ms |
| url-rtsp-…-live-avox264 (H264) | PASS | headless 起播 84ms |
| url-http-…-hls-m3u8 | PASS | |
| url-http-…-live-ts | PASS | |
| url-q-…-sherpa-mp4 (本地文件) | PASS | |
| magnet (tests/test_probe.gd) | 未跑 | 依赖外网 tracker |
| ZLM WebRTC 拉 (webrtcplaytest) | 未跑 | W3 专项 |

命令模板：

```bash
python script/testenv/push_streams.py
D:/Work/godot/godot.exe --headless --path platform/godot/tools -s res://tests/test_url.gd -- <url> --timeout-ms=25000
```

## W1 — Windows Unity / UE

| case | 结果 | 命令 |
|------|------|------|
| Unity 冒烟 (dll 加载/组件创建) | 待装编辑器 | `platform/unity/test_unity.ps1 -UnityProject <私仓工程>` |
| Unity 软解播放 (avox_electron.mp4) | 待装编辑器 | 同上 (内置断言: 帧尺寸+2s 进度) |
| Unity 各 GPU 后端 (D3D11/D3D12/Vulkan) | 未跑 | avox-unity 私仓 demo |
| UE 冒烟 + 软硬解 (H264/HEVC) | 未跑 | avox-ue 私仓 `run_verify.py` |

## W2 — Android 真机（Redmi K70 Ultra）

前提: `python build_android_godot.sh` 重出含 avox:// 深链的 APK。

| case | 驱动 | 结果 |
|------|------|------|
| rtsp-h264 / rtsp-h265 | `am start -d "avox://rtsp://<LAN>:554/live/avox264"` | 未跑 |
| rtmp | `avox://rtmp://<LAN>:1935/live/avox264` | 未跑 |
| hls | `avox://http://<LAN>/live/avox/hls.m3u8` | 未跑 |
| http-ts | `avox://http://<LAN>/live/avox264.live.ts` | 未跑 |
| 本地文件 (push 进设备) | `avox:///sdcard/...` 或 intent.getData 文件 | 未跑 |
| magnet (外网) | `avox://magnet:?xt=...` | 未跑 |
| 硬解/软解开关 × 上列 | 设置页切换后重跑 | 未跑 |

判定: `adb logcat -d | python script/testenv/collect_verdicts.py -`（main.gd 自动输出 case=url-play）。

## W0 — 桌面底座 (2026-09-09)

| case | 结果 | 备注 |
|------|------|------|
| ctest avox_tests | PASS | build/windows/avox, FFmpeg9+新webrtc 产物 |
| functest 关键项 | 未跑 | |

## W3 — WebRTC 专项 (Windows 先行)

2026-09-09 拉流实测 (`webrtcplaytest`, URL 已支持 argv[1] 传入, 默认仍 live/test):

| case | 结果 | 备注 |
|------|------|------|
| ZLM 信令拉流 H264 (live/test 720p) | PASS | 出图 1280x720, exit=0, 全程 15.0s |
| ZLM 信令拉流 H265 (live/avox) | PASS | 出图 1392x880, exit=0, 走 FFmpeg9 hevc 软解 (POC 报错为首帧前正常现象) |
| close 时序 (bisect) | 数据点 | delete→~RtcPlayer 析构间阻塞 ~10.7s, 之后拆卸 0.4s, 不再挂死 |
| Godot headless 拉流 H264 (test_rtc.gd) | PASS | first_video_frame 2537ms, conn=connected, 退出正常 |
| Godot headless 拉流 H265 (test_rtc.gd) | PASS | first_video_frame 874ms; **quit 后退出延迟分钟级**: teardown 全打完仍不退出, 期间 taskkill /F 一度失败, 数分钟后自行退出; headless 无 RenderingDevice(GPU直通跳过) → 排除 VK 导入路径, 与 avox-godot 仓 test_rtc_nogpu 假设互证 |
| Godot UI 拉流测试页 (tools/src/webrtc) | PASS | webrtc://127.0.0.1/live/avox264 输入自动转信令原链; 内置信令 first_frame 4112ms, 自定义信令 (GDScript 镜像 TestSdpOb: open_rtc→local_sdp→POST→set_remote_sdp) 2842ms, 判定行 case=rtc-ui; 页面入口 hub 卡片 |
| 发送方向 (sendOnly) | 发布 API + ZLM `type=push` | 未跑 (`webrtcpull` 需摄像头) |
| 断流自动重连 (杀推流端) | `setAutoReconnect` + `--stop` | 未跑 |
| DataChannel 回环 | `sendDataChannel`/`onDataChannelMsg` | 未跑 |
| 弱网 (丢包/限速) | clumsy 模拟 | 未跑 |
| 1h 长稳 + getFps/getLossRate/getRttMs 采样 | 脚本化 | 未跑 |
| 玻璃到玻璃延迟 | 源端打点 vs 呈现 | 未跑 (产出营销数字) |

## W5 — macOS (Mac M2 / macOS 26.1, 2026-09-09 实测)

构建: `AVOX_BUILD_TYPE=Release AVOX_CMAKE_ARGS=-DAVOX_ENABLE_SAMPLES=ON python3 build_mac.py`
（Windows 侧 `push_streams.py --lan-ip=192.168.68.245` 供流, Mac 拉 LAN）。
FFmpeg 9.0.1 IO 用 `avox-verify` 无头离屏 app（`AVOX_IO_PLAN=ffmpeg`, 15s 定长），
WebRTC 用仓内样例 `rtcplayheadless`（判定行 case=webrtc-play）。

| case | 结果 | 备注 |
|------|------|------|
| ctest avox_tests | PASS | build/macos/avox |
| ffmpeg9 rtsp-h264 | PASS | playing, fps 24.6, VideoToolbox 硬解 + Metal |
| ffmpeg9 rtsp-h265 | PASS | playing, fps 28.3 |
| ffmpeg9 hls (h265) | PASS | playing, fps 28.3 |
| ffmpeg9 http-ts (h264) | PASS | playing, fps 29.9 |
| ffmpeg9 rtmp (h264) | PASS | 修 rtmp 传 `timeout` 隐式 listen 后通 (fps 28.9), 修前必 `open input failed` |
| webrtc 无头拉流 h264/h265 | PASS | rtcplayheadless: conn=connected, 首帧, loss 0, rtt 0-2ms, EXIT=0 |
| webrtc 有窗口拉流 (3 路) | PASS | macrtcplaytest: 远端画面上屏 + 抓帧出真实远端帧; 帧率见下表 |

WebRTC 拉流 5 秒采样（`macrtcplaytest` 每 5 秒打点, 退出汇总; 全部 loss=0, EXIT=0）：

| 流 | 源属性 | 时长 | fpsAvg (min~max) | rttAvg (min~max) |
|----|--------|------|------------------|------------------|
| `live/test` | 1280x720 H264 30fps | 45s | **30.4** (30~31) | 1ms (0~2) |
| `live/avox264` | 1524x726 H264 30fps | 60s | **9.9** (8~17) | 6ms (4~21) |
| `live/avox` | 1392x880 H265 30fps | 60s | **9.1** (8~12) | 8ms (4~16) |

- 早前记录的 `rtt=313ms` **不复现**: 那是连上后立刻取的单点瞬时值(RTCP 未收敛), 11 点采样全在 4~16ms。
- 非 720p 那两路 WebRTC 只有 9fps, 而**同一路流走 RTSP 有 24.6/29.3fps**(见上表), 丢包 0、CPU 3%,
  故与源帧率无关, 是 WebRTC 路径特有。两路宽高均非 16 对齐(1524x726 / 1392x880), 待推一路
  1280x720 的自有源对照才能定性; 另一个候选是 `getFps()` 统计渲染帧, 而无音频时钟
  (样例 `setAudioDirection(inactive)`)时视频出帧节奏被 jitter buffer 带偏。**未定位。**
| macplaytest 窗口播放 (rtsp-h264) | PASS | 自建 NSWindow + CAMetalLayer, playing/pos 19.8s/fps 31.6; VNC 里肉眼确认出图 |
| screenShot 抓帧存 PNG | PASS | h264/zlmediakit 与 h265/ffmpeg 两路都出真帧 (1280x720 RGBA); 修 MetalRender 两处后才通, 详见下方 |
| 退出码 | PASS | 曾经 EXIT=134: `libmk_api.dylib toolkit::Logger::~Logger` → `LogContextCapture::~LogContextCapture` 抛 `recursive_mutex lock failed`; 根因是 Apple 静态链没有 DllMain(DETACH) 去跑 `AvoxManager::clean()`(内含 `mk_env_release`), Logger 单例撑到静态析构期。现 zl io init 泄漏一份 Logger 引用, zlmediakit/ffmpeg 两种 IO 均 EXIT=0 |

macOS 上编样例需显式 `-DAVOX_ENABLE_SAMPLES=ON`（默认仅 Win/Linux 开），且 Apple 静态
libavox.a 必须整档 `WHOLE_ARCHIVE`，否则 `AVOX_REGISTER_MODULE` 的静态注册被 ld 丢弃
（`createWebRtcPlayer()` 返回 null 直接段错误）。

看画面（Mac 无显示器/远程时）：mac 端已开屏幕共享（`kickstart -activate`, launchd 监听 5900），
Windows 用 VNC 客户端连 `192.168.68.219:5900`；窗口程序用样例 `macplaytest`：

```bash
# 普通 URL 播放 (IMediaPlayer)
build/macos/avox/install/aarch64/Release/macplaytest.app/Contents/MacOS/macplaytest \
  rtsp://<LAN>:554/live/avox264 [超时秒 0=关窗为止] [ffmpeg|zlmediakit] [存图路径]
# WebRTC 拉流 (IRtcPlayer, 走内置 ZLM 信令, 远端画面上屏)
build/macos/avox/install/aarch64/Release/macrtcplaytest.app/Contents/MacOS/macrtcplaytest \
  "http://<LAN>/index/api/webrtc?app=live&stream=avox264&type=play" [超时秒] [存图路径]
# 窗口内按 p 存一张; 给了存图路径则出图后每 3 秒覆盖存一张(SSH 里按不了键, 靠轮询看)
```

坑：`open`/双击 .app 启动等于不带参数，默认 URL 是 `rtsp://127.0.0.1:554/live/avox264`，
而 mac 上没有本地 ZLM ⇒ 空窗口无画面，且拉不到流时播放器 tick 仍 yield 自旋吃满一个核
（`swtch_pri` + `MediaPlayer::collectStatus/compateIoTime`，待优化）。要看画面必须带 LAN 地址。

`screenShot` 在 Apple 上原先必失败，两处叠加：`MetalRender::fetchFrame` 拿 `[metalLayer
nextDrawable]`（一张全新未绘制的 drawable，抓到清屏色；且取了不 present 还占空池子），改为
记住上一帧真正画过的目标纹理 + `framebufferOnly=NO`（否则 drawable 纹理不能当 blit 源）；
`getBytes:bytesPerRow:` 传的是 `format.rowPitch`，而 avox 约定 rowPitch=0 即紧凑，
Metal 收到 0 只写第一行 ⇒ 整张全 0（看图器显示成白/透明），这条离屏路径同样中招。

**已知回归（待修）**：定时截图会把渲染压到一半并卡死在低帧率。同一路 `live/test`
对照实测：带截图(每 3 秒一张) fps 30 → **掉到 17.0 钉死**，不带截图**稳定 30~31**，
两者 loss 均为 0、CPU 3%。三个原因叠加 ——
① `VideoRender::screenShot` 同步阻塞调用方(`future.wait_for(1s)`)等渲染线程回填；
② `MetalRender::fetchFrame` 每次新建 MTLTexture + blit + `waitUntilCompleted`，
每次插一个全 GPU 同步点并读回 3.5MB；
③ 为修抓帧引入的 `lastTargetTexture` **常驻持有一张 drawable 纹理**，而 CAMetalLayer
drawable 池默认只 3 个，被长期占掉一个后 `nextDrawable` 更易等待（这最能解释「掉到 17
之后钉死」而非抖动）。拟改为仅在抓帧请求置位时把画面 blit 进一张复用的离屏纹理，
drawable 用完立即放手，并把 PNG 编码挪出热路径。**在此之前定时截图只用于远程无 VNC
时看画面，不要用于帧率/流畅度测量。**

### W5 — macOS 复测: 帧布局契约批次 (2026-09-10, b8b1138)

Mac 同步到 b8b1138（帧布局立契约/onFrame 换签名 packed 透传/yuvouttest 等 10 个提交），
增量构建 Release+SAMPLES，Windows 侧 push_streams 供流。结论：除 onFrame 外全部正常。

| case | 结果 | 备注 |
|------|------|------|
| 修复: avox_tests Apple 链接失败 | 已修(未提交) | d8d7f77 把 VideoBuffer.cpp 直编进测试, 其 `__APPLE__` 分支用 CoreVideo 而测试目标不链 framework → 链接挂; Windows 无感, Mac 上 ctest 必挂。tests/CMakeLists.txt 补 `-framework CoreVideo` |
| 修复: yuvouttest Apple 不编译 | 已修(未提交) | samples 门控在 Apple 跳过整套 functest; samples/CMakeLists.txt 的 Apple 分支单独 add_executable(yuvouttest) |
| ctest avox_tests | PASS | 新布局契约单测 8 用例 56 断言全绿 (form 引用准入/to 零拷贝/往返还原) |
| yuvouttest rtsp-h264 (1524x726 非对齐) | **FAIL** | 转码录制 ✓ (rec.mp4 ffmpeg 解码校验干净) + screenShot ✓ (PNG 内容正确); 但 **onFrame 收 0 帧** → case=yuvout FAIL, EXIT=1 |
| onFrame Metal 缺口 (根因定位) | 待修 | `SurfaceRenderVk::getCpuFrameBuffer` 只咨询 vkVideoRender, packed 透传只在 `VkVideoRender` 实现(d78c35f); Mac 无 Vulkan 走 MetalRender, `VideoRender::getCpuFrameBuffer` 基类返回 false ⇒ Apple 上 onFrame 永不触发(硬解软解都一样)。Windows 默认有 Vulkan 掩盖此缺口。修复方向: MetalRender 实现 getCpuFrameBuffer (YUV 平面 packed 下载) |
| macplaytest rtsp-h264 回归 | PASS | playing fps 27.6, shot=ok, EXIT=0 |
| macplaytest rtsp-h265 回归 | PASS | playing fps 33.2, shot=ok, EXIT=0 |
| rtcplayheadless webrtc 拉流回归 | PASS | conn=1, firstFrame=1, loss=0, rtt=18ms, EXIT=0 |

## W4 — 稳定/边界 & W5 — iOS

见 `doc/` 后续补充; iOS 前置任务: 同步 avc_library ios 目录 + 重跑 `build_ios.py --flavor=commercial` 定位链接失败。
